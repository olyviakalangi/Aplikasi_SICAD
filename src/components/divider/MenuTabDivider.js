import { View, Text } from "react-native";
import React from "react";
import tw from "../../lib/tailwind";

const MenuTabDivider = () => {
  return <View style={[tw`h-20 w-full`]} />;
};

export default MenuTabDivider;
